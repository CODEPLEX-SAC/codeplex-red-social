import React, { useState } from "react";
import BotonesGuardar from "../../../compartido/ui/BotonesGuardar";
const inputCls = "w-full px-[14px] py-[10px] border border-[var(--border-color)] rounded-[var(--radius-sm)] text-[13.5px] bg-[var(--white-color)] text-[var(--text-dark)]";
const cardCls = "bg-[var(--white-color)] rounded-[var(--radius-md)] border border-[var(--border-color)] p-6 flex flex-col gap-5";
export default function SecHabilidades({ form, setForm, onCancelar, onGuardar, guardado }) { const [habilidadInput, setHabilidadInput] = useState(""); const habilidadesDelPerfil = form.habilidades ?? []; const agregarHabilidad = () => { const habilidad = habilidadInput.trim(); if (!habilidad || habilidadesDelPerfil.includes(habilidad)) return; setForm((prev) => ({ ...prev, habilidades: [...habilidadesDelPerfil, habilidad] })); setHabilidadInput(""); }; const eliminarHabilidad = (habilidad) => setForm((prev) => ({ ...prev, habilidades: habilidadesDelPerfil.filter((item) => item !== habilidad) })); return <div className={cardCls}><h4 className="text-[15px] font-bold text-[var(--text-dark)] m-0">Habilidades</h4><div className="flex gap-2"><input className={`${inputCls} flex-1`} value={habilidadInput} onChange={(e) => setHabilidadInput(e.target.value)} /><button onClick={agregarHabilidad} className="px-4 py-[10px] rounded-[var(--radius-sm)] text-[13px] font-semibold text-white border-none cursor-pointer" style={{ background: "var(--primary-color)" }}>+ Añadir</button></div>{habilidadesDelPerfil.map((h) => <span key={h} className="mr-2">{h}<button onClick={() => eliminarHabilidad(h)} className="ml-1">×</button></span>)}<BotonesGuardar onCancelar={onCancelar} onGuardar={onGuardar} guardado={guardado} /></div>; }

